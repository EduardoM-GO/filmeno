import 'package:flutter/material.dart';

class ImagemConteudoComponent extends StatelessWidget {
  const ImagemConteudoComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
