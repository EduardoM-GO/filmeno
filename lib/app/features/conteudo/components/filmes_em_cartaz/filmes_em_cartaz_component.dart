import 'package:flutter/material.dart';

class FilmesEmCartazComponent extends StatelessWidget {

  const FilmesEmCartazComponent({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}